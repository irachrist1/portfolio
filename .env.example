UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional: site URL for metadata (defaults to production domain in code)
# NEXT_PUBLIC_SITE_URL=https://christian-tonny.dev

# Optional: GitHub token for `npm run activity:generate` (higher rate limits, private repos)
# Create a fine-grained PAT with Contents: read on repos you track in app/data/repositories.json
# GITHUB_TOKEN=
